# Status snapshot — 15 September 2026

**Research / pre-menu. H10 guest-state coherence and H11 scheduler/POLL host semantics are closed with PASS. The first N6 native-validation attempt stopped during the build before link or game execution, so no new native progression, visible Main Menu or playable build is verified for this initiative.**

The private canonical branch records H10 and H11 as consumed host proofs. H11 established one selector/current-state producer on the real launch/claim path, bounded priority/contention admission, release/reschedule, and the POLL 0/1/2/3 truth table without forcing `FOUND_WORKLOAD`.

| Evidence | What is supported | What it does not establish |
|---|---|---|
| H10 final host integration | Guest-state coherence, owner/generation identity, writer participation and bounded lifetime behavior passed the protected host matrix | Native game behavior or menu |
| H11 scheduler host proof | Selection/current semantics, admission, capacity, release/reschedule and POLL behavior passed Normal/ASan/UBSan host gates | A successful native scheduler observation |
| N6 native-validation campaign | One authorized build attempt reached the runtime integration compile and exposed a missing diagnostic include-path dependency | Link, game execution, scheduler progression or UI |
| Protected post-audit | PPU, JobQueue, runtime, historical/reference and prior H10 sets remained hash-consistent after N6 closure | Race freedom or full-game correctness |

N6 is **consumed and closed as a build blocker**, with build/link/run counts **1/0/0**. No retry or second native campaign is authorized by this public repository. The next private engineering action is a separate host/tooling phase to make the existing diagnostic include dependency explicit and prove the build recipe before any fresh native authorization.

Current pinned references used for the H11/N6 delta check:
- `sp00nznet/twistedmetal` — `7858472a75ddba7b527242723a9443003759a614`
- `sp00nznet/ps3recomp` — `fddf580d48b4c71b2fa2a5ac6356a706bf873c35`
- `RPCS3/rpcs3` — `1b569fde4d2dc8a57531bef8a310bf113e2c1324`

The same-title public port is substantially farther ahead visually, including native title geometry and later RSX work; those achievements are reference material and are **not** counted as achievements of this initiative. No directly relevant upstream head delta was found for the N6 build blocker.

The exact redistributable host-source/build closure remains open under PUB-001. This public repository intentionally omits proprietary game material, private workstation paths, raw private traces and unreviewed private source.

Machine-readable source: `data/status.json`. Historical test counts and sanitizer variants are not port-completion percentages.
