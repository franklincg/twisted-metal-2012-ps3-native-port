# Status snapshot — 12 September 2026

**Research / pre-menu. H10 guest-state coherence remains open. Combined Windows integration is pending. No playable build or visible Main Menu is verified for this initiative.**

This status uses maintainer-supplied reports and a separately-read development dispatch. The dispatch still authorizes host integration work and prohibits native-game build/link/run and the next native campaign. It is not evidence that the final integration has run or passed.

| Evidence | What is supported | What it does not establish |
|---|---|---|
| Windows standalone model: 121 distinct checks in normal/ASan/UBSan | Reported Taskset layout/ownership contracts on model fixtures | Full runtime integration, scheduler or menu |
| Earlier Windows integration logs: 39 checks per variant | Historical host result, not repeated here | Current combined upstream/lifecycle candidate |
| Linux upstream component: 68 checks per variant | Component/accessor checks with fixture context/wake | Actual complete Windows path |
| Linux GETLLAR property: 36 checks per variant | Same observation used for LS and reservation snapshot in extracted case | Atomic 128-byte read against all writers or race freedom |

These suite counts are not additive as full-game coverage and sanitizer variants are not distinct tests. The currently open work is lifetime/retirement, coherent lock/writer participation, exact ABI compatibility, combined Windows host validation and source-export/build closure.

Historical native runs reported graphics/storage activity and progression past earlier context/dispatch defects, but not a verified menu for this initiative. No new native run is authorized by this public publication task. Scheduler selection remains a separate unresolved track; no workload result is forced.

The selected historical sources under research/h10_reference are review inputs. They are not an integrated production candidate. A newer public runtime does not automatically remove the local layout/lifetime problems.

Machine-readable source: data/status.json. The public summary omits private machine paths, raw logs, game data and internal operational instructions.
